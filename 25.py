#def p(x):                                                                   поиск простых
  #  return x > 1 and all(x % i != 0 for i in range(2, int(x**0.5) + 1))
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


def div(x):
    d = set()
    for i in range(2, int(x**0.5) + 1):
        if x % i == 0:
            d.add(i)
            d.add(x // i)
    return sorted(d)

for x in range(135790, 163228 + 1):
    d = div(x)
    if sum(d) > 460000:
        print(len(d), sum(d))
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

def div(x):#ввел значение m
    d = set()
    for i in range(2, int(x**0.5) + 1):
        if x % i == 0:
            d.add(i)
            d.add(x // i)
    return sorted(d)

for i in range(700000,1000000):
    d = div(i)
    if d:
        m = max(d) + min(d)
    else:
        m = 0
    if m % 10 == 8:
        print(i, m)
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

from fnmatch import *      #маска числа

for x in range(0, 10**9, 17):
    if fnmatch(str(x), '23?456?89'):
        print(x, x // 17)
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


from fnmatch import *        # маска и делители числа

def div(x):
    d = set()
    for i in range(1, int(x**0.5) + 1):
        if x % i == 0:
            d.add(i)
            d.add(x // i)
    return sorted(d)

for x in range(10**6):
    if fnmatch(str(x), '?8*8*?8') and x % 6 == 0 and x % 7 == 0 and x % 8 == 0:
        print(x, sum(div(x)))
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


from fnmatch import *             #   маска плюс делитель нечетный

def div(x):
    d = set()
    for i in range(1, int(x**0.5) + 1):
        if x % i == 0:
            d.add(i)
            d.add(x // i)
    return sorted(d)

for x in range(0, 10**7, 217):
    if fnmatch(str(x), '27?7*'):
        print(x, sum([i for i in div(x) if i % 2 != 0]))
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

b = []                             # маска числа с вопросами и в какой то системе можно вот так и в убывающем порядке

for c1 in '0123456789abcdef':
    for c2 in '0123456789abcdef':
        a = int(f'1{c1}ded{c2}ced', 16)
        if a % 121 == 0:
            b += [(a, a//121)]

for i in b[::-1]:
    print(*i)
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------



from fnmatch import *         #маска числа только с четными и нечетными в фнматче изи лайфхак

for x in range(0, 10 ** 8, 2023):
    if fnmatch(str(x), '11[02468]??[13579]11'):
        print(x, x // 2023)
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

from fnmatch import *

for x in range(0, 10**8, 206):
    if fnmatch(str(x), '123*[13579][02468]56'):
        print(x, x// 206)
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

from fnmatch import *

for x in range(700001, 710000):
    if x % 13 == 0:
        if not fnmatch(str(x), '*0??3*') and not fnmatch(str(x), '*4??2') and not fnmatch(str(x), '*1*'):
            print(x, sum(map(int, str(x)))) # сумма значений разрядов числа
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


def p(x):
    return x > 1 and all(x % i != 0 for i in range(2, int(x**0.5) + 1))

def div(x):
    d = set()
    for i in range(2, int(x**0.5) + 1):
        if x % i == 0:
            d.add(i)
            d.add(x // i) #  38621   158934
    return sorted(d)
ans = []
for x in range(158928, 345293):
    d = [i for i in div(x) if p(i)]
    if len(d) == 3:
        if d[0] * d[1] * d[2] == x:
            ans.append(x)
print(len(ans), min(ans))
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------


def div(x):
    d = set()
    for i in range(2, int(x**0.5) + 1):
        if x % i == 0:
            d.add(i)
            d.add(x // i)
    return sorted(d)

for x in range(10000001, 10001000):
    d = div(x)
    if len(d) > 1:
        s = d[-1] + d[-2]
        if 0 < s < 10000:
            print(x, s)

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------